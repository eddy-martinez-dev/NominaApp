<Window x:Class="NominaApp.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="NominaApp - Empleados" Height="500" Width="800">
    
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Encabezado + botón de carga -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="Lista de Empleados" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
            <Button Content="Cargar Empleados"
                    Command="{Binding CargarEmpleadosCommand}"
                    Margin="16,0,0,0" Padding="10,4"/>
        </StackPanel>

        <!-- Estado de carga / error -->
        <StackPanel Grid.Row="1" Margin="0,0,0,12">
            <TextBlock Text="Cargando..." Foreground="Gray"
                       Visibility="{Binding EstaCargando, Converter={StaticResource BoolToVisibilityConverter}}"/>
            <TextBlock Text="{Binding MensajeError}" Foreground="Red"
                       Visibility="{Binding MensajeError, Converter={StaticResource StringToVisibilityConverter}}"/>
        </StackPanel>

        <!-- Tabla de empleados -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding Empleados}" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="120"/>
                <DataGridTextColumn Header="Apellido" Binding="{Binding Apellido}" Width="120"/>
                <DataGridTextColumn Header="Cédula" Binding="{Binding Cedula}" Width="120"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="180"/>
                <DataGridTextColumn Header="Estado" Binding="{Binding Estado}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
